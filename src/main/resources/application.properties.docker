# Database Configuration for Docker
# Using H2 (embedded, easy for Docker)
db.url=jdbc:h2:mem:membershipdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
db.driver=org.h2.Driver
db.username=sa
db.password=

# Server Configuration
server.port=8080
server.host=0.0.0.0

# Logging
logging.level=INFO
